# ========================================================
# KisanDirect Environment Configuration (SIH 2026)
# ========================================================

# Server Port
PORT=5000
NODE_ENV=development

# Database Connection (SQLite by default for zero-friction local setup, PostgreSQL ready)
DATABASE_URL="file:./dev.db"
# PostgreSQL Connection (Alternative):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kisandirect?schema=public"

# JWT Authentication
JWT_SECRET="kisandirect_sih2026_super_secret_jwt_key_987654"
JWT_EXPIRES_IN="7d"

# External API Placeholders (Mock services implemented for prototype)
MAPS_API_KEY="mock_google_maps_key_demo"
PAYMENT_API_KEY="mock_razorpay_key_demo"
SMS_API_KEY="mock_fast2sms_key_demo"
VOICE_API_KEY="mock_google_speech_key_demo"

# Frontend URL for CORS
FRONTEND_URL="http://localhost:5173"
